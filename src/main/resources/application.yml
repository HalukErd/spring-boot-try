spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/spring_boot_try
    username: postgres
    password: dockerpassword
  jpa:
    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

    hibernate:
      ddl-auto: update